1) Does ANSI recognize the data type DATE?
Ans: Yes, ANSI SQL recognizes the DATE data type. In the ANSI SQL standard, the DATE data type is used to store date values, including year, month, and day.

2) Which subdivision of SQL is used to insert values in tables
Ans:  INSERT INTO table_name (column1, column2, ...)
      VALUES (value1, value2, ...);
